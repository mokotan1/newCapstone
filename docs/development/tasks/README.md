# 기능 작업 기록

기능마다 폴더를 만든다.

```text
docs/development/tasks/<feature-id>/index.md
docs/development/tasks/<feature-id>/<task-id>.md   # 위임한 작업만
```

`index.md`에는 목표, AC, 제외, 파일 소유, 부모 구현 증거, 위임 여부, 다음 단계를 적는다.
템플릿만 복사해 두고 실행하지 않은 패킷을 만들지 않는다.
